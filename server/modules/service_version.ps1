#Write-Host service_version.ps1 "This feature has not been added."
# sV 

# Version detection of a service, not currently implemented.
function service_version() {
    param(
        [PSCustomObject]$hostObj
    )
    Write-Host "Service versioning is not supported"
}
